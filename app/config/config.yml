imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }
    - { resource: "@MainBundle/Resources/config/services.yml" }
    - { resource: "@CommonBundle/Resources/config/services.yml" }

framework:
    #esi:             ~
    #translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    #serializer:      { enable_annotations: true }
    templating:
        engines: ['twig']
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    form:
        resources: ['bootstrap_3_layout.html.twig']

# Doctrine Configuration
doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    filters:
        cssrewrite: ~

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }

hwi_oauth:
    firewall_names: [secured]
    resource_owners:
        google:
            type:          google
            client_id:     "%google_client_id%"
            client_secret: "%google_client_secret%"
            redirect_uri:  ""
            scope: "https://www.googleapis.com/auth/userinfo.profile https://www.googleapis.com/auth/userinfo.email https://www.googleapis.com/auth/calendar"


# Pagination
knp_paginator:
    template:
        pagination: KnpPaginatorBundle:Pagination:twitter_bootstrap_v3_pagination.html.twig

parameters:
    locale: en
    mainURL: http://docs.org
    enable_cache_permissions: false
    google_client_id: 453435184655-vrs29j06pqnen74osshu6vnun59ickkt.apps.googleusercontent.com
    google_client_secret: ChL7MU_TULDgOgauEjaMM8CF

    google_api_key: AIzaSyCCFpLmpRP5-4BSTdRPFNchS3BwEmYRjSk
    google_api_key_server: AIzaSyCoCt9Z-XwUDAik1gsyKOuMvAQoUomNleI
    docs.api: http://api-docs.org
    docs.apiKey: 098f6bcd4621d373cade4e832627b4f6

#   exports file directory
    export_files_dir: %kernel.root_dir%/Resources/exports
